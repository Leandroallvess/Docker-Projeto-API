version: "3.8"

services:
  api:
    build: ./api
    ports:
      - "3000:3000"
    networks:
      - app-network

  web:
    build: ./web
    ports:
      - "3001:80"
    depends_on:
      - api
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
